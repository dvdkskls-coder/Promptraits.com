[build]
  publish = "build"
  command = "CI=false npm run build"

[functions]
  directory = "functions"
  node_bundler = "esbuild"

[functions."*"]
  timeout = 26

[[plugins]]
  package = "@netlify/plugin-functions-install-core"
